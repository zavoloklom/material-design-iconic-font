default:
  description: 'Default (production) build'
  tasks:
    - prod
prod:
  description: 'Production build'
  tasks:
    - 'clean:prod'
    - 'less'
    - 'copy:font'
    - 'copy:animatecss'
    - 'copy:highlight'
    - 'pleeease'